engine-strict=true

# React Native / Expo native builds (expo run:android/ios) require a flat
# node_modules layout. pnpm's default symlinked layout makes Gradle codegen
# tasks fail with "A problem occurred starting process 'command 'node'/'cmd''".
node-linker=hoisted
